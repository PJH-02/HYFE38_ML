name: ETF Rank Backtest 2024 KOFR Resume

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/etf-rank-backtest-a0-a5.yml"
      - "KOFR_20260508.xlsx"
      - "testing/*.py"
      - "testing/regime_weights.csv"
      - "testing/backtest_state/chunk_state_2024_*.json"

permissions:
  contents: write
  actions: write

concurrency:
  group: etf-rank-backtest-2024-kofr-state
  cancel-in-progress: false

env:
  REGIME_CSV_PATH: regime_weights.csv
  KOFR_XLSX_PATH: ../KOFR_20260508.xlsx
  OUT_ROOT: backtest_state/out_2024_kofr_a0_a6_k10
  PRIMARY_STATE_PATH: backtest_state/chunk_state_2024_a3_a4.json
  SECONDARY_STATE_PATH: backtest_state/chunk_state_2024_a2a_a2b.json
  START_DECISION_DATE: "2024-01-05"
  END_DECISION_DATE: "2026-05-07"
  TOP_K_VALUES: "10"
  MONTHS_PER_RUN: "6"

jobs:
  prepare-ranks:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      phase: ${{ steps.plan.outputs.phase }}
      should_run: ${{ steps.plan.outputs.should_run }}
      start_month: ${{ steps.plan.outputs.start_month }}
      end_month: ${{ steps.plan.outputs.end_month }}
      first_month: ${{ steps.plan.outputs.first_month }}
      last_month: ${{ steps.plan.outputs.last_month }}
      state_path: ${{ steps.plan.outputs.state_path }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip pandas numpy openpyxl

      - name: Rebuild rank panel
        working-directory: testing
        run: |
          python -u 01_make_market_corr_rank.py
          python -u 02_make_flow_price_rank.py
          python -u 03_make_rotation_rank.py
          python -u 04_merge_rank_panel.py

      - name: Determine next phase and chunk
        id: plan
        working-directory: testing
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p backtest_state

          python -u determine_next_backtest_chunk.py \
            --rank-panel rank_panel.csv \
            --state-path "$PRIMARY_STATE_PATH" \
            --months-per-run "$MONTHS_PER_RUN" \
            --start-date "$START_DECISION_DATE" \
            --end-date "$END_DECISION_DATE" > primary.env

          primary_should="$(grep '^should_run=' primary.env | cut -d= -f2-)"
          if [ "$primary_should" = "1" ]; then
            phase="primary"
            cp primary.env selected.env
            state_path="$PRIMARY_STATE_PATH"
          else
            python -u determine_next_backtest_chunk.py \
              --rank-panel rank_panel.csv \
              --state-path "$SECONDARY_STATE_PATH" \
              --months-per-run "$MONTHS_PER_RUN" \
              --start-date "$START_DECISION_DATE" \
              --end-date "$END_DECISION_DATE" > secondary.env
            secondary_should="$(grep '^should_run=' secondary.env | cut -d= -f2-)"
            if [ "$secondary_should" = "1" ]; then
              phase="secondary"
              cp secondary.env selected.env
              state_path="$SECONDARY_STATE_PATH"
            else
              phase="final"
              cp secondary.env selected.env
              state_path="$SECONDARY_STATE_PATH"
            fi
          fi

          cat selected.env
          echo "phase=$phase" >> "$GITHUB_OUTPUT"
          echo "state_path=$state_path" >> "$GITHUB_OUTPUT"
          cat selected.env >> "$GITHUB_OUTPUT"

      - name: Show plan
        run: |
          echo "phase=${{ steps.plan.outputs.phase }}"
          echo "should_run=${{ steps.plan.outputs.should_run }}"
          echo "start_month=${{ steps.plan.outputs.start_month }}"
          echo "end_month=${{ steps.plan.outputs.end_month }}"
          echo "available=${{ steps.plan.outputs.first_month }}..${{ steps.plan.outputs.last_month }}"

      - name: Upload rebuilt ranks
        uses: actions/upload-artifact@v4
        with:
          name: rebuilt-ranks-${{ github.run_id }}
          path: |
            testing/market_rank.csv
            testing/flow_rank.csv
            testing/flow_rank_metadata.json
            testing/flow_rank_no_selected_actor_report.csv
            testing/rotation_rank.csv
            testing/rank_panel.csv
            testing/regime_weights.csv
          if-no-files-found: error
          retention-days: 14

  non-llm-backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    needs: prepare-ranks
    if: needs.prepare-ranks.outputs.phase != ''
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip pandas numpy openpyxl

      - name: Download rebuilt ranks
        uses: actions/download-artifact@v4
        with:
          name: rebuilt-ranks-${{ github.run_id }}
          path: testing

      - name: Run A0 A1 A5 from 2024-01-05 decision
        working-directory: testing
        run: |
          mkdir -p "$OUT_ROOT"
          python -u A0_equal_weight.py \
            --panel rank_panel.csv \
            --out-root "$OUT_ROOT" \
            --start-date "$START_DECISION_DATE" \
            --end-date "$END_DECISION_DATE"
          python -u A1_rule_based_rank_allocator.py \
            --panel rank_panel.csv \
            --out-root "$OUT_ROOT" \
            --top-k "$TOP_K_VALUES" \
            --start-date "$START_DECISION_DATE" \
            --end-date "$END_DECISION_DATE"
          python -u A5_bayesian_winner_loser_allocator.py \
            --panel rank_panel.csv \
            --out-root "$OUT_ROOT" \
            --top-k "$TOP_K_VALUES" \
            --start-date "$START_DECISION_DATE" \
            --end-date "$END_DECISION_DATE"

      - name: Upload non-LLM state
        uses: actions/upload-artifact@v4
        with:
          name: etf-rank-non-llm-state-${{ github.run_id }}
          path: |
            testing/backtest_state/out_2024_kofr_a0_a6_k10/A0/
            testing/backtest_state/out_2024_kofr_a0_a6_k10/A1_k10/
            testing/backtest_state/out_2024_kofr_a0_a6_k10/A5_k10/
          if-no-files-found: error
          retention-days: 14

  primary-llm-backtest-chunk:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    needs: [prepare-ranks, non-llm-backtest]
    if: needs.prepare-ranks.outputs.phase == 'primary'
    name: Primary LLM ${{ matrix.llm_strategy }} ${{ needs.prepare-ranks.outputs.start_month }}..${{ needs.prepare-ranks.outputs.end_month }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        llm_strategy: [A3, A4]
    env:
      NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
      LLM_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
      LLM_CONFIG_PATH: .no_local_llm_config.json
      LLM_PROVIDER: nvidia
      LLM_BASE_URL: https://integrate.api.nvidia.com/v1/
      LLM_CHAT_COMPLETIONS_URL: https://integrate.api.nvidia.com/v1/chat/completions
      LLM_MODEL: z-ai/glm4.7
      LLM_TIMEOUT_SECONDS: "180"
      LLM_MAX_TOKENS: "2048"
      LLM_MAX_RETRIES: "12"
      LLM_RETRY_SLEEP_SECONDS: "30"
      LLM_SUCCESS_SLEEP_SECONDS: "2"
      LLM_RESPONSE_FORMAT: '{"type":"json_object"}'
      LLM_CHAT_TEMPLATE_KWARGS: '{"enable_thinking":false}'
      LLM_FAIL_ON_FALLBACK: "1"
      LLM_VALIDATION_RETRIES: "6"
      LLM_PROGRESS_INTERVAL: "10"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip pandas numpy openpyxl

      - name: Validate NVIDIA secret
        run: |
          if [ -z "$NVIDIA_API_KEY" ]; then
            echo "NVIDIA_API_KEY repository secret is required."
            exit 1
          fi

      - name: Download rebuilt ranks
        uses: actions/download-artifact@v4
        with:
          name: rebuilt-ranks-${{ github.run_id }}
          path: testing

      - name: Restore existing committed state
        run: mkdir -p testing/backtest_state/out_2024_kofr_a0_a6_k10

      - name: Run six-month primary LLM backtest chunk
        working-directory: testing
        run: |
          python -u run_monthly_strategy_backtest.py \
            --strategy "${{ matrix.llm_strategy }}" \
            --rank-panel rank_panel.csv \
            --out-root "$OUT_ROOT" \
            --top-k "$TOP_K_VALUES" \
            --start-month "${{ needs.prepare-ranks.outputs.start_month }}" \
            --end-month "${{ needs.prepare-ranks.outputs.end_month }}" \
            --start-date "$START_DECISION_DATE" \
            --end-date "$END_DECISION_DATE"

      - name: Upload primary LLM state for commit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: etf-rank-llm-state-${{ matrix.llm_strategy }}-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10/${{ matrix.llm_strategy }}_k10/
          if-no-files-found: error
          retention-days: 14

  secondary-llm-backtest-chunk:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    needs: [prepare-ranks, non-llm-backtest]
    if: needs.prepare-ranks.outputs.phase == 'secondary'
    name: Secondary LLM ${{ matrix.llm_strategy }} ${{ needs.prepare-ranks.outputs.start_month }}..${{ needs.prepare-ranks.outputs.end_month }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        llm_strategy: [A2a, A2b]
    env:
      NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
      LLM_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
      LLM_CONFIG_PATH: .no_local_llm_config.json
      LLM_PROVIDER: nvidia
      LLM_BASE_URL: https://integrate.api.nvidia.com/v1/
      LLM_CHAT_COMPLETIONS_URL: https://integrate.api.nvidia.com/v1/chat/completions
      LLM_MODEL: z-ai/glm4.7
      LLM_TIMEOUT_SECONDS: "180"
      LLM_MAX_TOKENS: "2048"
      LLM_MAX_RETRIES: "12"
      LLM_RETRY_SLEEP_SECONDS: "30"
      LLM_SUCCESS_SLEEP_SECONDS: "2"
      LLM_RESPONSE_FORMAT: '{"type":"json_object"}'
      LLM_CHAT_TEMPLATE_KWARGS: '{"enable_thinking":false}'
      LLM_FAIL_ON_FALLBACK: "1"
      LLM_VALIDATION_RETRIES: "6"
      LLM_PROGRESS_INTERVAL: "10"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip pandas numpy openpyxl

      - name: Validate NVIDIA secret
        run: |
          if [ -z "$NVIDIA_API_KEY" ]; then
            echo "NVIDIA_API_KEY repository secret is required."
            exit 1
          fi

      - name: Download rebuilt ranks
        uses: actions/download-artifact@v4
        with:
          name: rebuilt-ranks-${{ github.run_id }}
          path: testing

      - name: Run six-month secondary LLM backtest chunk
        working-directory: testing
        run: |
          python -u run_monthly_strategy_backtest.py \
            --strategy "${{ matrix.llm_strategy }}" \
            --rank-panel rank_panel.csv \
            --out-root "$OUT_ROOT" \
            --top-k "$TOP_K_VALUES" \
            --start-month "${{ needs.prepare-ranks.outputs.start_month }}" \
            --end-month "${{ needs.prepare-ranks.outputs.end_month }}" \
            --start-date "$START_DECISION_DATE" \
            --end-date "$END_DECISION_DATE"

      - name: Upload secondary LLM state for commit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: etf-rank-llm-state-${{ matrix.llm_strategy }}-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10/${{ matrix.llm_strategy }}_k10/
          if-no-files-found: error
          retention-days: 14

  commit-primary-state:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [prepare-ranks, non-llm-backtest, primary-llm-backtest-chunk]
    if: needs.prepare-ranks.outputs.phase == 'primary'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Download non-LLM state
        uses: actions/download-artifact@v4
        with:
          name: etf-rank-non-llm-state-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10

      - name: Download A3 state
        uses: actions/download-artifact@v4
        with:
          name: etf-rank-llm-state-A3-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10/A3_k10

      - name: Download A4 state
        uses: actions/download-artifact@v4
        with:
          name: etf-rank-llm-state-A4-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10/A4_k10

      - name: Update primary chunk state
        run: |
          python - <<'PY'
          import json, os, time
          from pathlib import Path
          state_path = Path("testing") / os.environ["PRIMARY_STATE_PATH"]
          state_path.parent.mkdir(parents=True, exist_ok=True)
          state = {
              "last_completed_start_month": os.environ["START_MONTH"],
              "last_completed_end_month": os.environ["END_MONTH"],
              "months_per_run": int(os.environ["MONTHS_PER_RUN"]),
              "start_decision_date": os.environ["START_DECISION_DATE"],
              "end_decision_date": os.environ["END_DECISION_DATE"],
              "phase": "primary_a3_a4",
              "out_root": "testing/backtest_state/out_2024_kofr_a0_a6_k10",
              "run_id": os.environ["GITHUB_RUN_ID"],
              "updated_at": time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime()),
          }
          state_path.write_text(json.dumps(state, ensure_ascii=False, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          PY
        env:
          START_MONTH: ${{ needs.prepare-ranks.outputs.start_month }}
          END_MONTH: ${{ needs.prepare-ranks.outputs.end_month }}

      - name: Commit primary state
        id: commit_state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add testing/backtest_state
          if git diff --cached --quiet; then
            echo "No backtest state changes to commit."
            echo "committed=0" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "ci: save 2024 kofr A3/A4 state through ${{ needs.prepare-ranks.outputs.end_month }}"
          git pull --rebase --autostash origin "${{ github.ref_name }}"
          git push origin "HEAD:${{ github.ref_name }}"
          echo "committed=1" >> "$GITHUB_OUTPUT"

      - name: Trigger next phase or chunk
        if: steps.commit_state.outputs.committed == '1'
        env:
          GH_TOKEN: ${{ github.token }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          curl -fsSL -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/etf-rank-backtest-a0-a5.yml/dispatches" \
            -d "{\"ref\":\"${REF_NAME}\"}"

  commit-secondary-state:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [prepare-ranks, non-llm-backtest, secondary-llm-backtest-chunk]
    if: needs.prepare-ranks.outputs.phase == 'secondary'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Download non-LLM state
        uses: actions/download-artifact@v4
        with:
          name: etf-rank-non-llm-state-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10

      - name: Download A2a state
        uses: actions/download-artifact@v4
        with:
          name: etf-rank-llm-state-A2a-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10/A2a_k10

      - name: Download A2b state
        uses: actions/download-artifact@v4
        with:
          name: etf-rank-llm-state-A2b-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10/A2b_k10

      - name: Update secondary chunk state
        run: |
          python - <<'PY'
          import json, os, time
          from pathlib import Path
          state_path = Path("testing") / os.environ["SECONDARY_STATE_PATH"]
          state_path.parent.mkdir(parents=True, exist_ok=True)
          state = {
              "last_completed_start_month": os.environ["START_MONTH"],
              "last_completed_end_month": os.environ["END_MONTH"],
              "months_per_run": int(os.environ["MONTHS_PER_RUN"]),
              "start_decision_date": os.environ["START_DECISION_DATE"],
              "end_decision_date": os.environ["END_DECISION_DATE"],
              "phase": "secondary_a2a_a2b",
              "out_root": "testing/backtest_state/out_2024_kofr_a0_a6_k10",
              "run_id": os.environ["GITHUB_RUN_ID"],
              "updated_at": time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime()),
          }
          state_path.write_text(json.dumps(state, ensure_ascii=False, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          PY
        env:
          START_MONTH: ${{ needs.prepare-ranks.outputs.start_month }}
          END_MONTH: ${{ needs.prepare-ranks.outputs.end_month }}

      - name: Commit secondary state
        id: commit_state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add testing/backtest_state
          if git diff --cached --quiet; then
            echo "No backtest state changes to commit."
            echo "committed=0" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "ci: save 2024 kofr A2a/A2b state through ${{ needs.prepare-ranks.outputs.end_month }}"
          git pull --rebase --autostash origin "${{ github.ref_name }}"
          git push origin "HEAD:${{ github.ref_name }}"
          echo "committed=1" >> "$GITHUB_OUTPUT"

      - name: Trigger next phase or chunk
        if: steps.commit_state.outputs.committed == '1'
        env:
          GH_TOKEN: ${{ github.token }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          curl -fsSL -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/etf-rank-backtest-a0-a5.yml/dispatches" \
            -d "{\"ref\":\"${REF_NAME}\"}"

  final-a6-report:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    needs: [prepare-ranks, non-llm-backtest]
    if: needs.prepare-ranks.outputs.phase == 'final'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip pandas numpy matplotlib openpyxl

      - name: Download rebuilt ranks
        uses: actions/download-artifact@v4
        with:
          name: rebuilt-ranks-${{ github.run_id }}
          path: testing

      - name: Download non-LLM state
        uses: actions/download-artifact@v4
        with:
          name: etf-rank-non-llm-state-${{ github.run_id }}
          path: testing/backtest_state/out_2024_kofr_a0_a6_k10

      - name: Run A6 and final report
        working-directory: testing
        run: |
          python -u A6_bayesian_online_expert_allocator.py \
            --mode selected \
            --top-k 10 \
            --rank-panel rank_panel.csv \
            --out-root "$OUT_ROOT/A6" \
            --expert-out-root "$OUT_ROOT" \
            --start-date "$START_DECISION_DATE" \
            --end-date "$END_DECISION_DATE"
          python -u build_final_backtest_report.py \
            --out-root "$OUT_ROOT" \
            --rank-panel rank_panel.csv \
            --report-dir backtest_state/final_report_2024_kofr_a0_a6 \
            --models A0,A1,A2a,A2b,A3,A4,A5,A6

      - name: Commit final A6 report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add testing/backtest_state
          if git diff --cached --quiet; then
            echo "No final backtest changes to commit."
            exit 0
          fi
          git commit -m "ci: save final 2024 kofr A0-A6 report"
          git pull --rebase --autostash origin "${{ github.ref_name }}"
          git push origin "HEAD:${{ github.ref_name }}"
